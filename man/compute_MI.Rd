% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_MI.R
\name{compute_MI}
\alias{compute_MI}
\title{Compute mutual information (MI) matrix from continuous data}
\usage{
compute_MI(data, px = NULL, ex = NULL, h = NULL)
}
\arguments{
\item{data}{Numeric matrix (rows = variables, columns = samples). Required.}

\item{px}{Optional list of marginal distributions (one numeric vector per
variable), as produced by \code{get_distributions()}. Computed if not
supplied.}

\item{ex}{Optional list of bin edges (one numeric vector per variable).
Required if \code{px} is supplied.}

\item{h}{Optional numeric vector of per-variable entropies, as produced by
\code{get_entropy()}. Computed if not supplied.}
}
\value{
MI matrix, normalized with respect to joint entropy.
}
\description{
Computes an MI matrix where each entry (i, j) is the mutual information
between variables i and j. Intended for datasets small enough to fit in
memory, where no parallel/batch processing is needed.
}
\examples{
\dontrun{
mi <- compute_MI(data)
mi <- compute_MI(data, px = px_data, ex = ex_data)
}

}
\seealso{
compute_MI_batch, normalize_MI
}
