% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_MI_batch.R
\name{compute_MI_batch}
\alias{compute_MI_batch}
\title{Compute mutual information (MI) matrix from continuous data, in batches}
\usage{
compute_MI_batch(
  data,
  file,
  px = NULL,
  ex = NULL,
  h = NULL,
  batch = 1000,
  pp = FALSE,
  fetch = TRUE,
  cont = FALSE
)
}
\arguments{
\item{data}{Numeric matrix (rows = variables, columns = samples).}

\item{file}{Output file path (without extension) to back the result
matrix. Two files will be created: \code{<file>.bin} and
\code{<file>.desc}.}

\item{px}{Optional list of marginal distributions, as produced by
\code{get_distributions()}. Computed if not supplied.}

\item{ex}{Optional list of bin edges. Required if \code{px} is supplied.}

\item{h}{Optional numeric vector of per-variable entropies, as produced
by \code{get_entropy()}. Computed if not supplied.}

\item{batch}{Batch size. Default \code{1000}.}

\item{pp}{Whether to use parallel processing via \code{compute_MI_PP()}.
Default \code{FALSE}.}

\item{fetch}{Whether to wait for parallel processing to finish before
returning. Only relevant when \code{pp = TRUE}. Default \code{TRUE}.}

\item{cont}{Whether to continue from a previous, unfinished run (attaches
to the existing backing file instead of reallocating). Default
\code{FALSE}.}
}
\value{
Invisibly, a \code{big.matrix} object containing the disk-backed
MI matrix. The matrix data are stored at \code{<file>.bin}.
}
\description{
Computes a normalized MI matrix where each entry (i, j) is the MI
between variables i and j. Intended for datasets too large to comfortably
hold as a single in-memory result matrix: this function does not require
the full MI matrix to live in RAM, computing it in batches and storing
the result in a disk-backed matrix (via \pkg{bigmemory}). Optionally uses
parallel processing (via the \pkg{future} ecosystem).
}
\examples{
# Create a small example dataset
set.seed(123)
data <- matrix(rnorm(300), nrow = 3)

# Store the disk-backed MI matrix in a temporary directory
file <- file.path(tempdir(), "netscope_mi")
mi <- compute_MI_batch(data, file, batch = 2)

# Access the resulting matrix
mi[, ]

}
\seealso{
\code{compute_MI}, \code{normalize_MI}
}
